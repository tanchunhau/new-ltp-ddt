# @name Check if /dev/mtd entries exist 
# @desc Check if /dev/mtd entries exist
# @requires qspi_master/mtd
# @setup_requires qspi
QSPI_S_FUNC_MTD_ENTRY source "common.sh"; install_modules.sh "qspi"; ls /dev/mtd* && exit 0 || exit 1





